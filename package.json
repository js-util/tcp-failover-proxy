{
	"name": "@js-util/tcp-failover-proxy",
	"version": "1.0.5",
	"description": "Setup a TCP proxy server, with automated failover - note this is not a loadbalancing proxy.",

	"keywords": [
		"tcp",
		"proxy",
		"server",
		"failover"
	],

	"main": "TCPFailoverProxy.js",
	"scripts": {
		"start": "node commandline.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},

	"author": "picocreator",
	"license": "MIT",
	"private": false,

	"repository": {
		"type": "git",
		"url": "https://github.com/js-util/tcp-failover-proxy.git"
	},

	"engines": {
		"node": ">=8.0.0"
	},

	"dependencies": {
		"@js-util/array-shuffle": "^1.0.3",
		"@js-util/net-socket-connect": "^1.0.0",
		"@js-util/two-way-socket-pipe": "^1.0.0"
	}
}
